<div class="nc-container">
<div class="nc-card">
  <h2 class="nc-title">🚗 出張登録（<%= @machine.name.capitalize %>）</h2>
  <p class="nc-subtitle">マシンを持ち出す時間帯にチェックを入れてください</p>
  <%= render 'machine_schedule_calender' %>
</div>
</div>
