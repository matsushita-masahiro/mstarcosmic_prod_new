===============================
<%= @user.name %>様から
===============================

      下記予約を承りました。
      施術者：<%= Staff.find_by(id: @reserve.staff_id).name %>
      内容：<%= @reserve.selected_machine %>
      <%= @reserve.reserved_date.strftime("予約日時 %Y-%m-%d >> ") %><%= @reserved_space %>〜（<%=@frames.to_i*30%>分）
      備考 <%= @reserve.remarks %>
      -----------------------------------
      m☆COSMIC 事務局
      HP : <%= ENV['USER_EMAIL'] %>
      -----------------------------------